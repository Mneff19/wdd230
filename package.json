{
  "name": "project",
  "version": "1.0.0",
  "description": "Project for the WDD 230 class",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webkit",
    "css:watch": "node-sass --output-style compressed styles -o styles -w dist",
    "css:watch-lessons": "node-sass --output-style compressed lessons/lesson2/styles/ -o lessons/lesson2/styles/ -w dist",
    "css:watch-chamber": "node-sass --output-style compressed chamber/styles/ -o chamber/styles/ -w dist"
  },
  "author": "Matthew Neff",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.2",
    "node-sass": "^8.0.0",
    "normalize.css": "^8.0.1",
    "webkit": "^0.0.0"
  }
}
